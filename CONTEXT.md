# CONTEXT

이 리포에서 쓰는 말의 뜻. 코드·문서·화면 문구는 이 표기를 따른다. 뜻이 바뀌면 여기부터 고친다.

## 서비스와 데이터

**개편 전 / 개편 후 (before / after)**:
2026년 10월 광주 시내버스 노선 개편의 전과 후. 화면 표기는 반드시 「개편 전」「개편 후」.
_Avoid_: 기존/신규, 현행/변경.

**노선안 (route plan)**:
시가 공표한 개편안의 노선 목록. 원천은 `data/source/광주권역 개편전 노선안.csv`(111행)와
`광주권역 개편후 노선안.csv`(119행). 행마다 버스번호·기점·종점·상행/하행 정류장 수·정류장 이름 순서.
**좌표와 정류장 ID가 없다** — 정류장은 이름으로만 나온다.

**비교표 (replacement table)**:
시가 공표한 「노선개편 전·후 비교표」. `data/source/노선개편 전후 비교표.csv`(103행). 개편 전 번호 하나에
개편 후 대체 노선 번호가 쉼표로 여럿. 대체 관계의 **유일한 출처**이며 우리가 계산하지 않는다.
번호는 앞자리 0을 무시하고 읽는다 — 순환01 행만 「1」이라 적혀 있고 나머지 행과 시 노선도는 「01」이다.
_Avoid_: 대체표, 매핑표.

**대체 노선 (replacement route)**:
비교표가 개편 전 노선 하나에 붙여 둔 개편 후 노선. 하나일 수도(45행), 여덟일 수도(첨단94) 있다.
_Avoid_: 후속 노선, 대응 노선.

**종류 (kind)**:
개편 후 노선의 앞말. 직행·급행·간선·지선 넷이고, 228·419·518·1187은 종류가 없다.
개편 후 노선은 **종류 + 번호**로 구별한다. 급행 03과 간선 03은 다른 노선이다(기·종점은 같고
급행이 정류장을 건너뛴다). 같은 번호가 둘인 것은 03·05·06 세 쌍뿐.
비교표는 급행을 뜻할 때만 「급행03」이라 적고, 숫자만 적은 것(「03」「1003」)은 **급행 아닌 것**으로
읽는다. 1003은 급행이지만 그 번호가 하나뿐이라 숫자만으로 정해진다.
개편 전 번호의 앞말(순환·좌석·문흥·두암…)은 종류가 아니라 번호의 일부다.

**노선 이름 (route name)**:
노선 하나를 화면·파일·코드에서 부르는 하나의 표기. 띄어쓰기 없음.
개편 전은 CSV의 버스번호 그대로 — 「문흥18」「순환01」「두암81(각화초교.장등동)」「228(구151.화순사평)」.
개편 후는 종류+번호(+방면) — 「간선01」「급행03」「지선97」「지선97(빛그린산단출근)」「228」.
시 노선도가 「간선 01」처럼 띄어 쓴 것도 우리는 붙여 쓴다. 앞자리 0은 시 표기대로 둔다(「간선1」 아님).
_Avoid_: 번호만 적기(「03」은 급행·간선 둘 중 무엇인지 모른다), 「간선 01」(띄움).

**방면 (branch)**:
노선 하나가 노선안 CSV에서 행 여럿으로 갈라진 것. 같은 번호(개편 후는 같은 종류+번호)인데 운행 경로가 다르다.
개편 전: 두암81은 (각화초교.장등동)(각화초교.장등마을)(무등파크.장등동)(무등파크.장등마을) 4행.
111행 중 방면이 갈라진 번호는 6개(228·두암81·상무62·선운101·송정97·지원152), 14행 → 번호 103개.
송정97처럼 접미 없는 행과 「(봉정행)」 행이 섞인 것도 방면이다. 접미 없는 행의 방면 이름은 「기본」.
개편 후: 지선 97 하나뿐 — 지선 97과 지선97(빛그린산단출근)(편도, 출근 시간대). 급행 03·간선 03은 종류가
달라 방면이 아니다.
비교표는 번호 하나로만 적으므로, 표를 만들 때 방면과 대체 노선을 **둘 다** 골라야 하고, 대체 노선에
방면이 있으면 그것도 고른다. 대체 노선과 다른 축이다 — 섞어 쓰지 않는다.

**상행 / 하행 (up / down)**:
노선안 CSV의 두 정류장 목록. 기점→종점이 상행, 종점→기점이 하행. 순환 노선은 하행이 0개다
(개편 전 7행, 개편 후 1행). 개편 후 노선의 기·종점이 개편 전과 **반대**인 경우가 있어 상행끼리
그대로 맞대면 안 된다(ADR-0003).

**명칭 변경 (rename)**:
개편으로 이름만 바뀐 정류장. `data/source/명칭 변경 정류소.csv`(102행, 정류장 ID 단위).
옛 이름 하나가 새 이름 **여럿**으로 갈라지기도(법원입구 → 광주법원검찰역1번출구 / 2번출구),
여럿이 하나로 합쳐지기도(송암공단입구 3개 ID → 송암산단입구) 한다. 표에서는 **같은 정류장**으로
본다(ADR-0003).

**신설 정류소 (new stop)**:
개편 후에만 있는 정류장. `data/source/신설 정류소.csv`(68행). 자치구와 이름뿐, 좌표 없음.

**폐지 / 통폐합 / 이전**:
`data/source/통폐합이전정류소.csv`(16행)의 「구분」 열 값 셋. 통폐합 3(두 정류장을 하나로,
`A(B)` 표기는 B를 A로 흡수), 폐지 12(사유에 "인근 ○○ 정류소를 이용" 안내), 이전 1.

**정류장 좌표 (stop coordinates)**:
CSV에는 없다. `data/source/stops.csv`(4,746개, WGS84)에서 가져온다. 광주 BIS API에서 받으며 출처와
반입 절차는 ADR-0007(ADR-0004를 대체). 이름을 대조하기 전에 `data/name_canon.json`을 통과시킨다.
좌표 없는 정류장 57개(신설 56 + 광주교대역2번출구)는 비워 둔다. **지어낸 좌표를 넣지 않는다.** 장소 탭의 경로
계산만 추정 좌표(아래)를 쓴다.

**노선 형상 (route shape)**:
노선 하나가 방향 하나로 도로 위에 그리는 선(2026-09-04). 그 방향의 정류장을 순서대로 이은 **차도 경로**이며
개편 전·후를 같은 방법으로 낸다. 시가 공표한 것은 정류장의 순서뿐이라 **실제 운행 경로가 아니다 — 추정이다.**
그래서 지도에서 **점(정류장)은 진하게, 선(형상)은 반투명하게** 그리고 지도 아래에 "선은 정류장을 순서대로 이은
차도 경로입니다. 실제 운행 경로와 다를 수 있습니다"를 적는다. 정류장 좌표(위)와 다르다 — 좌표는 시가 아닌
BIS가 준 사실이고, 형상은 우리가 낸 추정이다. 출처·반입 절차와 라이선스는 ADR-0009.
_Avoid_: 노선 경로(경로와 섞임), 폴리라인, 도로 형상.

**배차간격 (headway)**:
같은 노선의 버스가 한 정류장을 잇따라 지나는 사이의 시간(분). 개편 전 값은 시가 공표한 것이라
`data/source/route_headways.csv`에 그대로 있고, **개편 후 값은 우리가 추정한 것**이다(ADR-0010).
둘을 한 낱말로 부르되 화면과 답에서는 늘 「추정」이라 밝힌다. 방면이 갈라진 노선은 방면마다가 아니라
**번호 하나에 값 하나**다 — 두암81 네 방면이 각 30분이면 두암81의 배차간격은 30분이다.
_Avoid_: 배차, 배차 시간, 운행 간격.

**운행횟수 (trips)**:
하루에 그 노선을 달리는 **편도** 횟수. 상행과 하행을 따로 센다. 시가 공표한 총량은 개편 전 8394회,
개편 후 9355회이고 마을버스는 안 들어 있다. 배차간격과는 유효 운행시간으로 이어진다.
_Avoid_: 운행 대수, 배차 횟수.

**유효 운행시간 (effective span)**:
배차간격과 운행횟수를 잇는 보정 상수 `E`(608.6분). 시간표가 아니다 — 공표된 배차간격은 첫차~막차
내내가 아니라 촘촘한 때의 값이라, 하루를 그 배차로 채웠다 치면 몇 시간어치인지를 발표 8394회에서
거꾸로 푼 것이다. 그래서 첫차~막차(17시간쯤)보다 짧다.
_Avoid_: 운행 시간, 배차 시간대.

**밴드 · 확신 (band · confidence)**:
배차간격 추정을 **모형을 달리 골랐을 때** 어디까지 가는지로 감싼 구간이 밴드이고, 그 폭을 셋으로
끊은 것이 확신(높음 · 보통 · 낮음)이다. 통계적 신뢰구간이 아니다. 확신이 「낮음」인 노선은 점 추정을
내지 않고 범위로만 말한다.
_Avoid_: 신뢰구간, 오차범위.

**등급 (verdict)**:
노선 하나에 붙는 권고 넷 중 하나 — 「증차 필요」「재배치 검토」「현행 적정」「여력 있음」. 이 넷
말고는 나오지 않는다. 뜻과 문턱은 ADR-0010.
_Avoid_: 권고, 판정, 등급 점수.

**적합 배차간격 (fit headway)**:
「차량을 한 대도 안 늘리고 대기시간 총합을 가장 줄이는 배차」. 추정 배차간격이 「개편안대로면
이렇게 된다」라면 이것은 「같은 차량을 다시 나누면 이래야 한다」다. 둘을 섞어 쓰지 않는다.
계산은 제곱근 법칙 — 수요가 네 배인 노선의 배차는 1/4이 아니라 1/2이다(ADR-0011).
_Avoid_: 적정 배차, 권장 배차(「이 노선의 진짜 적정값」이 아니라 같은 차량을 다시 나눈 결과다).

**운용 대수 (buses)**:
그 배차를 유지하는 데 드는 첨두 차량 수 = 왕복시간 ÷ 배차간격. 노선마다 「지금 대수」와
「적합 대수」 둘이 있고, 그 **차이가 다른 노선에서 옮겨 와야 할 대수**다. 추론하지 않고 셈한다.
_Avoid_: 차량 대수(어느 쪽인지 안 밝힌 것), 인가 대수(시가 정한 수는 우리에게 없다).

**계산값 / 추론값 (computed / inferred)**:
빌드가 미리 계산해 `headway.json`에 박아 둔 값이 계산값이고, 화면의 배차간격 칸에 뜨는 것은
그것을 근거로 모형이 부를 때마다 다시 판단한 **추론값**이다(ADR-0011). 추론값은 매번 조금씩
다르되 표본 셋의 중앙값이라 한곳으로 모이고, 계산값의 밴드 밖으로는 나가지 않는다.
키가 없으면 계산값을 그대로 보이고 「?」 상자가 그렇다고 밝힌다.
_Avoid_: 예측값, 확정값.

**답변 뼈대 (answer skeleton)**:
`/headway`가 수치와 함께 주는, 수치가 박힌 문장 몇 줄. LLM은 말투를 바꿔도 이 문장의 **내용**을
바꿀 수 없다. 같은 노선을 여러 번 물었을 때 답이 한곳으로 모이게 하는 것이 이것이다.
_Avoid_: 템플릿, 프롬프트, 요약문.

## 화면

**장소로 찾기 / 노선번호로 찾기**: 첫 화면의 탭 둘. 이름 그대로 쓴다.
**장소 탭이 처음 열려 있다**(2026-09-04) — 시민이 먼저 묻는 것은 「내 길이 어떻게 달라지나」다.

**예시 검색 (example search)**:
장소 탭 입력칸 아래의 알약 셋. 누르면 두 칸이 채워지고 바로 비교가 돈다(2026-09-04).
채울 것이 둘이라 첫 화면이 막막한 것을 던다. 적는 말은 시민이 부르는 이름(「유스퀘어」·「첨단지구」)이고
좌표는 `stops.csv`에 실제로 있는 정류장 자리다 — 지어낸 좌표를 쓰지 않는다(ADR-0007). 이름과 좌표가
정확히 같은 곳일 필요는 없다: 장소 탭은 좌표로 비교하고 타고 내릴 정류장은 도보권 안에서 엔진이 고른다.
_Avoid_: 샘플, 추천 검색.

### 두 탭 공용 (2026-09-03 확정)

**자동완성 후보 (autocomplete candidates)**:
입력칸에 글자를 치면 아래에 뜨는 후보 목록. 고르면 값이 확정되고 결과가 뜬다. **두 탭 모두 1글자부터** 후보가 뜬다(2026-09-04). 노선번호 탭은 비교표의
번호 103개를 껍데기에 통째로 실은 `<datalist>`이고 브라우저가 첫 글자부터 스스로 좁힌다(정적·우리 스크립트 없음).
후보 줄은 값이 번호, 설명이 대체 노선 이름들 — 「지원152 — 간선18 · 지선10」. 목록에 없는 값을 넣으면 카드가 안 뜨고
안내문 "목록에 있는 번호를 고르세요"만 남는다.
장소 탭은 Worker가 받은 Kakao 장소 검색 결과(이름 · 주소, 동적 조각 — **1글자부터**, 노선안 정류장 범위 안, 5개).
자리가 같으므로 한 이름으로 부른다.
노선번호 탭에 표 필터는 없다 — 노선 개편 목록 표는 늘 103행 그대로이고 좁히기는 후보 목록이 맡는다.
_Avoid_: 표 필터, 번호 필터.

### 노선번호로 찾기 (2026-09-03 확정)

**노선 개편 목록 표 (route reform list)**:
첫 화면의 표. 비교표 103행 = 개편 전 번호 103개(방면 합친 것)와 그 대체 노선을 한 표에 놓았다. 한 줄을 누르면
노선 변화 카드가 뜬다. 화면 제목 「노선번호 개편안」. 정적(ADR-0002).
**처음에는 다섯 줄만 보이고 나머지는 접혀 있다**(2026-09-04) — 103줄이 다 서면 목록이 화면을 다 먹어 한 줄을
눌러도 결과가 어디에 떴는지 안 보인다. 표 아래 단추가 「나머지 98개 더 보기」 ↔ 「접기」로 바뀐다. 줄을 덜어 내지
않고 감추기만 하므로 열 너비가 흔들리지 않고 브라우저의 「페이지에서 찾기」도 접힌 줄을 찾는다. 여닫는 것은
숨긴 체크박스와 CSS다 — 탭 전환과 같은 수법이라 우리 스크립트가 없다(ADR-0001).
대체 노선이 하나도 없는 노선(두암181 하나)에는 **「노선 사라짐」**이라 적는다(2026-09-04). 「대체 노선 없음」은
「대체가 없을 뿐 노선은 남는다」로 읽히는데, 이 노선은 개편 후 노선안에 아예 없다.
_Avoid_: 대체 노선 없음, 폐선, 노선 목록 표, 노선 대조표.

**노선 변화 카드 (route change card)**:
번호를 확정했을 때 나오는 영역. 제목 「<번호(방면)> 노선 변화」, 대체 노선 목록, 버튼 줄(개편 전 방면 선택·대체 노선 선택·개편 후 방면 선택),
상행·하행 요약 칸, 노선 변화 표를 담는다. 파일 `route/{번호}.html`(ADR-0002).
_Avoid_: 노선 결과 카드, 노선 상세.

**개편 전 방면 선택 / 대체 노선 선택 / 개편 후 방면 선택 (before-branch pick / replacement pick / after-branch pick)**:
노선 변화 카드의 버튼 줄. 위는 「개편 전 방면 선택」 — 방면이 여럿인 6개 노선 카드에만 있다. 가운데는
「대체 노선 선택」 — 모든 카드에 있다. 아래는 「개편 후 방면 선택」 — 고른 대체 노선에 방면이 있을 때만
(지선 97) 나타난다 `[기본] [빛그린산단출근]`. 없는 줄은 카드에 아예 없다.
버튼에는 노선 이름을 적는다 — `[간선18] [지선10]`. 안내문은 "개편 전 방면을 고르세요" / "대체 노선을 고르세요" /
"개편 후 방면을 고르세요". 다 고르면 노선 변화 표 하나가 정해진다. 줄마다 축이 다르다 — 합쳐 부르지 않는다.
_Avoid_: 갈래 선택, 방면 선택(앞말 없이), 대체 방면 선택.

**노선 변화 표 (route change table)**:
개편 전 노선(방면 하나)과 대체 노선 하나의 정류장 목록을 순서를 지키며 맞댄 표. 열 여섯 —
`# · 개편 전 상행 정류장 · 개편 후 상행 정류장 · 개편 전 하행 정류장 · 개편 후 하행 정류장 · 비고`.
위에서 아래로 읽으면 버스가 가는 순서. 화면 제목은 「<번호(방면)> 노선 변화」, 예: 「두암81(각화초교.장등동) 노선 변화」.
하행 없는 노선은 하행 두 칸을 비우고 비고에 순환이면 "순환 노선 · 하행 없음", 편도(지선97(빛그린산단출근))면
"편도 운행 · 하행 없음". 파일 `route/{번호}/{개편 전 방면}/{대체 노선}/{개편 후 방면}.html`(ADR-0006).
규칙은 ADR-0003.
_Avoid_: 정류장 변화 표, 정류장 손실.

**유지 · 경유 제외 · 경유 추가 (kept · dropped · added)**:
노선 변화 표의 줄마다 붙는 상태 셋. 유지 = 개편 전후 모두 이 노선이 지남. 경유 제외 = 개편 전에만 지남.
경유 추가 = 개편 후에만 지남. **주어는 노선이다** — 경유 제외는 정류장이 없어진 것이 아니라 이 노선이 더는
안 지난다는 뜻이고, 다른 노선은 올 수 있다. 진짜 폐지는 비고에만 나온다.
_Avoid_: 안 옴, 새로, 폐지(비고의 「폐지」와 헷갈림), 신설(비고의 「신설」과 헷갈림).

**상행·하행 요약 칸 (up/down summary)**:
노선 변화 표 위에 같은 크기로 나란히 놓은 칸 여섯. 상행·하행 각각 유지/경유 제외/경유 추가의 개수.
문구 "상행 · 경유 제외 11곳".
_Avoid_: 개수 칸, 집계.

**비고 (note)**:
노선 변화 표의 여섯째 열. CSV 사실 넷만 적는다 — 명칭 변경("명칭 변경: 문흥고가 → 문화육교") · 통폐합
("통폐합: 서방사거리육교를 흡수") · 이전 · 신설. 구분 값만 적고 사유 문장은 마우스를 올리면 보이게 둔다.
둘 이상이면 " · "로 잇는다. 유지·경유 제외·경유 추가는 비고에 적지 않는다(줄 색과 요약 칸이 말한다).
통폐합 CSV의 「A(B)」는 B가 A에 흡수된 것이므로, 비고는 **B가 개편 전 열에 나오는 줄**에 "통폐합: A에 흡수"로
붙는다(2026-09-04). 통폐합 CSV가 폐지됐다는 정류장이 개편 후 노선안에 남아 있어도(서방사거리육교) 노선안을
고쳐 읽지 않는다 — 정류장 목록의 출처는 노선안 하나다.

**기·종점 정렬 (terminus alignment)**:
개편 전 상행이 대체 노선의 상행·하행 중 어느 쪽과 짝인지 정하는 것. 개편 후 노선의 기·종점이 개편 전과
반대인 쌍이 있어 필요하다. 방법은 셋 — ① 기·종점 이름 → ② 정류장 목록 겹침 → ③ `data/기종점정렬표.csv`에
사람이 적음. 뒤집어 맞댄 표는 아래에 "기점·종점이 반대라 뒤집어 맞댔습니다" 한 줄. 실측은 ADR-0003.
_Avoid_: 방향 맞대기, 방향 정렬.

**기·종점 정류장 대조 (stop matching)**:
기·종점 정렬로 방향을 맞춘 뒤, 개편 전과 개편 후의 정류장 목록을 순서를 지키며 한 줄씩 짝지어(최장 공통
부분열) 유지·경유 제외·경유 추가를 가르는 것. 명칭 변경 사전을 먼저 적용한다. 기·종점 정렬과 한 범주다 —
정렬은 방향, 대조는 줄.
_Avoid_: 줄 맞대기, 순서 정렬.

**노선 지도 (route map)**:
노선 변화 카드 위에서 개편 전 노선(방면 하나)과 고른 대체 노선의 **상행**을 겹쳐 그리는 지도(2026-09-04).
하행은 그리지 않는다 — 같은 길 건너편이라 겹치면 읽히지 않는다. 선 둘: 개편 전은 굵은 초록(`#19e225` — 타일
위에서 읽히도록 글자 초록보다 밝다, 2026-09-04)을 아래에,
대체 노선은 가는 파랑을 위에. 정류장 점은 유지 회색 · 경유 제외 빨강 · 경유 추가 파랑이고,
**바뀐 곳(경유 제외 · 경유 추가)만 큰 점**이라 눈이 달라진 자리에 먼저 간다(2026-09-04).
정류장 이름은 지도에 적지 않는다 — 100곳을 넘는 노선이 있어 다 적으면 글자가 지도를 덮는다. 표의 줄 색은 그대로
(경유 추가는 표에서 초록) — 지도의 초록은 「개편 전 선」, 표의 초록은 「경유 추가」로 뜻이 다르다는 것을
범례가 적는다. 선은 노선 형상(위)이고 브라우저가 따로 받으며, 정류장 점은 노선 변화 표 조각에 실린다
(표 하나 = 지도 하나). 그리는 것은 Kakao JS(ADR-0001·0005).
좌표 없는 정류장은 건너뛰고 앞뒤를 이으며 지도 아래 한 줄 "좌표 없는 정류장 N곳은 지도에 없습니다". 장소 탭의 경로 지도와 다르다 — 노선 전체를 그린다.
_Avoid_: 상세 지도, 경로선 지도.

### 장소로 찾기 (2026-09-03 확정 · Worker 설계 그릴링 Q1~Q15)

**노선망 (network)**:
개편 전 노선안 111행 또는 개편 후 노선안 119행이 이루는 노선·정류장 전체. 둘뿐이고, 경로는 늘 한 노선망
안에서만 찾는다.
_Avoid_: 네트워크, 노선 체계.

**경로 (journey)**:
한 노선망 안에서 출발 지점 → 승차 정류장 → (환승 정류장 …) → 하차 정류장 → 도착 지점으로 이어지는 하나의 길.
버스 구간 하나에 노선 하나, 구간은 최대 셋(환승 최대 2회). 승차 뒤 하차는 같은 방향(상행 또는 하행) 목록의
**뒤 순번**이어야 하고, 순환 노선도 목록 끝에서 처음으로 넘기지 않는다. 노선 조합이 같으면 승차·하차 정류장이
달라도 같은 경로로 보고 가장 좋은 것 하나만 남긴다.
_Avoid_: 노선(route와 섞임), 루트.

**상태 (journey status)**:
노선망 하나에서 찾은 결과의 등급 넷 — 환승 없음 · 환승 1회 · 환승 2회 · 경로 없음. 카드 머리에 적는다.
넷이 모두 환승 횟수 한 축의 말이라, 두 카드를 나란히 놓으면 몇 단계 달라졌는지가 바로 읽힌다(2026-09-04).
두 카드의 상태를 나란히 보는 것이 개편 전후 비교이며, 둘을 합친 판정 문장은 **두지 않는다**.
_Avoid_: 직행(등급 넷 중 하나만 다른 축의 말이 된다), 판정, 결과 등급.

**도보권 (walk radius)**:
지점에서 걸어가 탈 수 있다고 보는 정류장의 범위. 출발·도착 모두 500m, 가까운 순 8개까지. 출발지 500m 안에
정류장이 없으면 100m씩 넓혀 1,000m까지 찾고, 도착지는 500m로 고정한다. 출발·도착 두 지점이 500m 이하면 경로를
찾지 않고 "걸어갈 수 있는 거리"라고만 알린다.
_Avoid_: 반경(앞말 없이), 도보 거리(도보 합계와 섞임).

**환승 도보 (transfer walk)**:
한 노선에서 내려 다른 노선을 타기까지 걷는 거리. 같은 정류장이 아니어도 350m 안이면 환승으로 본다. 도보권과
값이 다르다 — 섞어 쓰지 않는다. 지도에서는 **인도를 따르는 선**으로 그린다(ADR-0009) — 정류장 쌍이 고정이라
미리 낼 수 있다. 출발·도착 도보는 지점이 무한해 그럴 수 없고 직선으로 둔다.
_Avoid_: 환승 반경, 환승 거리.

**추정 소요 시간 (estimated duration)**:
카드에 「예상 시간」으로 보이는 값. 시각표가 없어 정류장 하나 지날 때 110초, 도보 4km/h로 계산한 추정치이며
**배차 대기는 넣지 않는다.** 정류장당 시간은 정차·승하차·신호까지 든 값이다 — 달리는 시간만 센 20초로는 예상
시간이 실제의 3분의 1로 나왔다(2026-09-04). 도보 몫은 직선거리의 **1.3배**를 걷는다고 보고 재지만, 그 1.3배는
**시간에만** 쓴다 — 카드의 도보 합계와 도보권 반경은 직선 그대로다.

**순위를 매기는 값은 이것이 아니다.** 줄을 세울 때는 여기에 구간마다 **배차간격의 절반**을 기다린다고 보고
더한 값(`rank`의 `rankSeconds`)을 쓴다(2026-09-04). 배차간격을 모르는 노선(개편 전 순환01 하나)은 20분으로
본다 — 개편 전 공표값 110개의 중앙값이며, 0으로 두면 그 노선만 안 기다리고 타는 것이 되어 공짜로 이긴다.
대기를 화면 값에 안 더하는 까닭은 배차간격의 출처가 두 카드에서 다르기 때문이다(ADR-0010) — 더하면 견주라고
나란히 세운 두 숫자가 서로 다른 자로 잰 값이 된다. 그렇다고 순위에서까지 빼면 배차 37분 노선으로 갈아타는 길을
곧장 가는 길보다 빠르다고 말하게 된다. 그 대가로 **카드에 적힌 분이 순서와 어긋나 보일 수 있고**(32분이 39분
아래에 설 수 있다), 그 까닭은 카드 아래 각주 한 줄이 말한다. 순위는 **추정 소요 시간 → 환승 0회 → 환승 1회 → 도보 합계 → 환승 2회** 순이다(2026-09-04).
시간이 맨 앞이고 환승 횟수는 시간이 **똑같을 때**만 본다 — 그때는 적게 갈아타는 쪽이다. 시간이
소수점까지 있는 초라 뒤 넷은 좀처럼 안 불린다: 사실상 시간 하나로 줄이 서고, 갈아타지 않는 길이라도
1분 느리면 뒤로 간다. 밀려난 길은 「다른 경로 더 보기」가 받는다. 상수도 그 셈도
코드 한 곳(`worker/rules.js`)에 두고 자료가 들어오면 그곳만 바꾼다 — `rank`가 순위를 매길 때와 `search`가
가망 없는 후보를 버릴 때가 같은 셈을 써야 탐색이 버린 것이 순위에서 1등이 되는 일이 없다.
_Avoid_: 소요 시간(「추정」 없이), 실제 시간.

**경로 줄 (journey steps)**:
카드 위쪽에서 출발 지점 → 승차 → (환승 …) → 하차 → 도착 지점을 차례로 적는 부분. 양 끝에는 시민이 고른
**장소 이름**과 그 지점에서 걷는 거리를 적는다(이름은 요청에 실려 온다. 없으면 「출발 지점」·「도착 지점」).
버스 구간마다 노선 이름과 함께 **그 노선에서만 정해지는 값** — 배차간격과 지나는 정류장 수 — 을 적는다.
배차간격은 **출처가 둘이라 표기가 다르다.** 개편 전은 시가 공표한 값이라 「12분」이라 적고
(110개, `data/source/route_headways_with_stops.csv`), 개편 후는 시 공표 **총량을 노선별로 나눈 추정**이라
「약 12.6분(추정)」이라 적는다(119행 전부, ADR-0010). 같은 칸에 나란히 서므로 **안 가르면 시민이
추정을 공표값으로 읽는다.** 둘 다 없는 것은 개편 전 순환01 하나뿐이고 그때만 "정보 없음"이다.
_Avoid_: 타임라인, 여정, 경로 목록.

**지표 (metrics)**:
카드 아래쪽 수치 표 — **예상 시간 · 환승 · 노선번호 · 배차간격 · 도보 합계** 다섯 줄이 늘 같은 차례로 선다.
노선번호와 배차간격은 구간마다 달라지므로 **경로가 쓰는 값을 차례대로 「·」로 이어** 한 줄에 적는다 —
환승 2회면 노선 셋이 한 칸에 선다. 구간이 어디서 갈리는지는 경로 줄이 보이므로 지표는 견주기만 맡는다
(2026-09-04, 그날 오전의 「전체 값 셋만」 규칙을 대신한다). 정류장 수는 여전히 경로 줄에만 있다.
레이블을 왼쪽, 값을 오른쪽에 두어 개편 전·후 두 카드의 행이 늘 맞는다.
_Avoid_: 수치 줄, 지표 칩.

**추정 좌표 (estimated coordinates)**:
좌표 없는 정류장 57개(신설 56 + 광주교대역2번출구)에 경로 계산용으로만 붙이는 위치. 같은 노선의 앞뒤 정류장
좌표의 중점. `stops.csv`에는 쓰지 않으며 카드에 「추정 위치」라고 표시한다(ADR-0007 예외). 승차·하차·환승 후보와
통과 모두 허용한다.
_Avoid_: 지어낸 좌표(금지어와 섞임), 보정 좌표.

**개편 전 카드 / 개편 후 카드 (before card / after card)**:
나란히 놓인 카드 한 쌍. 각각 머리의 상태와 경로 줄, 지표를 보여 준다. 두 카드는 **짜임이 똑같고 바닥에서
정렬한다** — 지표가 같은 높이에 와야 시민이 눈을 옮기지 않고 견준다(2026-09-04). 경로 줄 길이가 달라
생기는 빈 자리는 메우지 않는다. 추정치·도보권·배차 출처를 알리는 문장은 카드 안이 아니라 **한 쌍 아래에
한 벌**로 둔다 — 카드마다 같은 문장이 서면 반복이고 카드 높이도 먹는다. 같은 까닭으로 다른 경로 카드도
카드 밖 아래에 선다(위 「다른 경로 카드」). Worker가 만드는
동적 조각(ADR-0001·0008).
도착 지점 도보권에 그 노선망의 정류장이 하나도 없으면 그 자리에 **카드 대신 안내 한 줄**이 선다. 상태가 아니라
카드가 없는 것이다 — 「경로 없음」(탈 수는 있는데 가는 길이 없다)과 「탈 곳이 없다」는 다른 말이기 때문이다.
_Avoid_: 경로 비교 카드, 경로 변화 카드, 비교 카드.

**다른 경로 카드 (alternative route card)**:
「다른 경로 더 보기」를 펴면 나오는 경로 최대 2개. 기본 경로와 **노선 조합이 다른 것**만.
각 경로에 「지도에 표시」 버튼이 있어 경로 지도에 올릴 것을 고른다. 경로 키만 실려 있고 조각은 처음 펼 때
`GET /journey/{id}`가 만든다. 폈다 접었다 할 수 있고, 접었다 다시 펴도 조각을 새로 받지 않는다(2026-09-04).
**개편 전·후 카드 안이 아니라 그 아래에 선다** — 카드 한 쌍은 지표를 맞추려고 서로 늘어나므로, 안에 있으면
한쪽만 펴도 옆 카드가 빈 채로 따라 늘어난다.
_Avoid_: 카드 상세, 경로 펼침.

**경로 키 (journey key)**:
경로 하나를 주소 한 조각에 적은 것. 「노선망 · 출발 지점 · 도착 지점 · 승차 STATION_NUM · 노선 이름 ·
(하차 · 승차 · 노선 이름)… · 하차 STATION_NUM」을 `|`로 이어 base64url로 적는다. 저장소가 없어 이 글자만으로
경로가 복원되고, 그래서 링크를 남에게 보내도 열린다. 방향과 순번은 안 적는다 — 승차·하차 줄과 노선이
정해지면 「같은 방향에서 승차보다 뒤 순번, 지나는 정류장이 가장 적은 짝」 하나로 정해진다.
두 지점은 스펙의 칸 목록에 없던 것을 더한 것이다 — 출발·도착 도보는 시민이 고른 지점에서 재는 값이라
번들에 없고, 그것이 빠지면 다른 경로 카드의 예상 시간이 기본 카드와 다른 자로 잰 값이 된다(2026-09-04).
_Avoid_: 경로 ID, 링크 키.

**경로 지도 (journey map)**:
개편 전·후 카드 위에서 두 노선망의 경로(출발 → 도착) 하나씩을 겹쳐 그리는 지도. 카드당 하나만 올라간다.
정류장 좌표는 Worker 조각에 실리고, **선은 노선 형상 파일(`shape/…`)을 브라우저가 따로 받아 승차부터 하차까지를
잘라 쓴다**(ADR-0009 결정 4, 2026-09-04). 못 받으면 정류장을 직선으로 잇는다. 그리는 것은 Kakao JS.
개편 전은 점선, 개편 후는 실선, 걷는 구간은 회색 점선이고
정류장마다 점을 찍는다. **점 크기는 둘** — 타고 내리는 곳은 큰 점(가운데가 흰 도넛)에 「가정류장 승차」처럼
이름 라벨이 붙고, 지나가는 곳은 작은 점에 라벨이 없다. **개편 전·후 경로가 같은 정류장을 쓰면 점 하나를
반씩 나눠** 두 색으로 칠한다 — 점 둘을 겹쳐 놓으면 한 색만 보인다(2026-09-04). 색은 노선 지도와 같은 초록·파랑을 쓴다. 버스 구간은 노선 형상(위)에서 승차부터
하차까지를 잘라 쓴다. 걷는 구간은 환승 도보(아래)만 인도를 따르고 출발·도착 도보는 직선이다 — 시민이 고른
지점은 미리 계산할 수 없다. 좌표 없는 정류장은 건너뛰고 앞뒤를 잇는다.
그릴 것이 없거나 Kakao JS 키가 없으면 지도 자리를 감춘다(2026-09-04).
노선번호 탭의 노선 지도와 다르다 — 경로를 그린다.
_Avoid_: 경로선 지도, 비교 지도.

## 구조

**정적 (static)**:
빌드 때 한 번 만들어 Cloudflare Pages에 올리는 파일. 요청마다 실행되는 코드가 없다.
페이지 껍데기, 노선 개편 목록 표, 그리고 **노선번호 탭의 결과 조각 전부**가 정적이다(ADR-0002).

**동적 (dynamic)**:
요청마다 서버(Worker)가 만들어 보내는 것. **장소 탭의 자동완성 후보·카드 한 쌍·다른 경로 카드**만 동적이다(ADR-0001).
Worker는 빌드가 만든 번들 JSON을 읽고, D1은 쓰지 않는다(ADR-0008).

**조각 (fragment)**:
페이지 전체가 아니라 한 영역만 담은 HTML 토막. htmx가 받아 `hx-target` 자리에 끼운다.
정적 조각(빌드 때 만든 파일)과 동적 조각(Worker가 만든 응답) 둘 다 있다.
_Avoid_: 파셜, 컴포넌트(React 뜻과 섞임).

**껍데기 (shell)**:
제목·탭·입력칸·안내처럼 데이터와 무관하게 늘 같은 페이지 부분. 정적.

**htmx**:
HTML 속성(`hx-get`, `hx-target`, `hx-trigger`)만으로 "이 URL을 받아 이 자리에 끼워라"를 시키는
라이브러리. 서버는 JSON이 아니라 HTML 조각을 돌려준다.

**Pages / Worker**:
Cloudflare의 부품 둘. Pages = 정적 파일 창고. Worker = 요청마다 실행되는 서버 코드(JavaScript). D1(SQLite DB)은
쓰지 않는다 — 데이터는 Worker 번들 JSON에 있다(ADR-0008). 역할 표는 `docs/architecture.md` §2.

**번들 JSON (bundle)**:
빌드가 CSV에서 만들어 Worker 코드와 함께 배포하는 데이터 파일. 정류장 · 노선 · 노선별 정류장 순서 · 환승 도보 안
정류장 쌍 · 노선 쌍별 환승 지점을 담는다. 요청 중에 바뀌지 않으며, 데이터가 바뀌면 다시 빌드해 배포한다.
_Avoid_: DB, 시드.

**Kakao 키 (REST / JS)**:
REST 키는 장소 검색용, Worker 환경 변수에만. JS 키는 지도용, 브라우저에 나가되 도메인 화이트리스트로 막는다(ADR-0005).

**빌드 스크립트 (build script)**:
배포 전에 한 번 도는 프로그램. CSV 6개와 `stops.csv`를 읽어 정적 조각 파일과 번들 JSON을 쓴다.
노선 변화 표의 규칙(명칭 사전·기·종점 정렬·기·종점 정류장 대조·비고)은 **전부 여기**에 산다(ADR-0002).

**명칭 사전 (rename dictionary)**:
명칭 변경 CSV에서 만든 옛 이름 → 새 이름 **집합**. 빌드 스크립트 1단계의 산출물이며 기·종점 정류장 대조와
번들 만들기가 함께 쓴다. 옛 이름 하나에 새 이름 여럿을 허용한다(ADR-0003 결정 2).

**모듈 이름 (module names)** — 코드에서 쓰는 영문 이름과 한글 용어의 대응. 파일·함수 이름은 이 표를 따른다.
모듈 지도 아트보드(`docs/canvas/modmap-*.dc.html`)의 이름과 같다. 새 세션에서 이 표만 읽고도 각 모듈이 무엇을 받아 무엇을 내는지
알 수 있도록 적는다. 장소 탭 설계(2026-09-03 그릴링)로 미확정 표시는 모두 걷혔다.

**① 빌드 스크립트 (build script)** — `tools/build`. 배포 전에 한 번 돈다. 입력은 `data/source/*.csv` 6개와
`data/source/stops.csv`, 그리고 이름 잇기 표 `data/name_canon.json`·기·종점 정렬표 `data/기종점정렬표.csv`.
출력은 `out/`(정적 파일)와 `worker/data.json`(번들 JSON). 노선번호 탭은 이것만으로 완성된다(ADR-0002).

| 코드 이름 | 한글 용어 | 하는 일 |
|---|---|---|
| `load` | — | CSV 6개를 읽어 행 레코드로 만든다. 정류장 구분자를 통일한다(개편 전 `▶`, 개편 후 `>`). 노선 표기를 (종류, 번호, 방면)으로 가른다 — `간선 01`→(간선, 1, 기본), `지선 97(빛그린산단출근)`→(지선, 97, 빛그린산단출근), 비교표 `1`·`급행03`·`03`→간선01·급행03·간선03. 규칙은 ADR-0006, 못 찾는 쌍 0 |
| `rename_dict` | 명칭 사전 | 「명칭 변경 정류소.csv」 102행에서 옛 이름 → 새 이름 **집합**을 만든다. 옛 이름 하나에 새 이름 여럿(법원입구 → 1번출구/2번출구), 여럿이 하나로(송암공단입구 ID 3개 → 송암산단입구) 모두 허용. `stop_match`와 `bundle`이 쓴다 |
| `branches` | 방면 | 「개편전 노선안.csv」에서 같은 번호의 행을 방면으로 나눈다. 개편 후도 같은 종류+번호의 행을 방면으로 나눈다. 개편 전 111행 → 번호 103개, 방면 여럿은 6개 번호(228·두암81·상무62·선운101·송정97·지원152) 14행. 개편 후 119행 → 118개, 방면 여럿은 지선97. 개편 전 방면 × 대체 노선 × 개편 후 방면이 노선 변화 표 하나의 단위 |
| `terminus_align` | 기·종점 정렬 | 표 하나(개편 전 방면 × 대체 노선 × 개편 후 방면, 205쌍)마다 개편 전 상행이 대체 노선의 상행·하행 중 어느 쪽과 짝인지 정한다. ① 기·종점 이름이 같거나 바뀌어 있음(92쌍) → ② 정류장 목록 겹침(LCS)이 뚜렷함(95쌍) → ③ `data/기종점정렬표.csv`에 사람이 적은 값(18쌍). 뒤집었는지 플래그를 남긴다. 실측 `tools/measure_direction.py` |
| `stop_match` | 기·종점 정류장 대조 | 짝지어진 두 정류장 목록을 순서를 지키며 한 줄씩 맞댄다(최장 공통 부분열). 명칭 사전을 먼저 적용해 이름만 다른 정류장을 「유지」로. 줄마다 유지 / 경유 제외 / 경유 추가를 매기고 상행·하행 요약 칸 숫자 여섯을 낸다. 첫 테스트: 문흥18 ↔ 간선18 = 상행 49·9·3, 하행 52·10·4(명칭 사전 적용 전에는 47·11·5 / 50·12·6이었다) |
| `notes` | 비고 | 줄마다 비고를 붙인다 — 명칭 변경(옛 이름 → 새 이름) · 통폐합 · 이전 · 신설. 출처는 명칭 변경·통폐합이전·신설 CSV 셋. 구분 값만 적고 사유 문장은 `title`. 하행 없으면 "순환 노선 · 하행 없음" 또는 "편도 운행 · 하행 없음". 상태 문구와 대신 서는 대체 노선은 적지 않는다(ADR-0003 개정) |
| `route_list` | 노선 개편 목록 표 | 비교표 103행 = 개편 전 번호 103개(방면 합친 것). 개편 전 번호와 대체 노선 이름, 행마다 `hx-get="route/{번호}.html"`. 화면 제목 「노선번호 개편안」 |
| `route_card` | 노선 변화 카드 | 번호 하나의 카드 조각. 제목 「<번호(방면)> 노선 변화」, 기·종점, 대체 노선 목록, 버튼 줄 셋(`hx-get="route/{번호}/{개편 전 방면}/{대체 노선}/{개편 후 방면}.html"`), 상행·하행 요약 칸, 노선 지도 자리, 노선 변화 표 자리(기본 방면·첫 대체 노선의 표를 미리 품음) |
| `route_geometry` | 노선 지도 | 노선 변화 표 하나(개편 전 방면 × 대체 노선 × 개편 후 방면)의 **상행** 두 목록을 `data/source/stops.csv` 좌표 배열로 바꿔 **표 조각** 안 `<script type="application/json" class="route-geometry">`에 넣는다(2026-09-04, 카드가 아니라 표 — 표 하나가 지도 하나). 대체 노선 쪽은 뒤집어 맞댔으면 하행 목록을 쓴다. 싣는 것은 `before`·`after`(선)·`stops`(점, `stop_match`의 줄에서)·`missing` 넷. 이름을 줄에 잇는 것은 `bundle`의 `StopIndex`를 그대로 쓰되 **추정 좌표는 안 받는다** — 좌표 없는 정류장은 건너뛰고 앞뒤를 이으며 개수를 `missing`으로 센다(ADR-0007). 같은 이름이 여러 곳인 것은(「금곡마을」 6줄, 55km) 자리로 묶은 뒤 **노선을 따라 가장 짧아지는 자리**를 고른다 — 다 평균 내면 들판에 점이 찍힌다(2026-09-04 실측 §6). 그리는 것은 브라우저 `map` |
| `shell` | 껍데기 | 두 탭 공용 `index.html` 템플릿. 제목·탭·입력칸·안내·빈 결과 영역, htmx·CSS·브라우저 스크립트 둘(`place.js`·`map.js`). Kakao 지도 SDK 태그는 빌드 환경 변수 `KAKAO_JS_KEY`가 있을 때만 넣는다 — 없으면 빌드는 그대로 성공하고 지도만 안 뜬다(ADR-0005). 데이터와 무관 |
| `render` | 조각 | 위 결과를 HTML 파일로 쓴다. `out/index.html`(껍데기 + 노선 개편 목록 표), `out/route/{번호}.html` × 103, `out/route/{번호}/{개편 전 방면}/{대체 노선}/{개편 후 방면}.html` × 205, 그리고 화면 자산 셋(`site.css`·`place.js`·`map.js`)을 그대로 옮긴다. 파일 하나 23KB 안팎 — 절반이 노선 지도 좌표다 |
| `bundle` | 번들 JSON | `load`·`rename_dict`·`branches`의 결과와 `data/source/stops.csv`로 `worker/data.json`을 쓴다. 표 다섯 — stops(STATION_NUM·ARS_ID·이름·lat·lng·추정 여부, **stops.csv 줄 단위**로 길 양쪽이 따로) · routes(노선망·노선 이름·배차) · route_stops(노선·방향·순번·이름·STATION_NUM, 이름 하나 → 그 이름의 줄 전부) · transfers(350m 안 정류장 쌍·거리) · route_links(노선 쌍당 최단 환승 지점 **1개**). 개편 후 이름은 명칭 사전으로 STATION_NUM에 잇고, 좌표 없는 57개는 앞뒤 중점의 추정 좌표. 노선안 정류장 bbox도 여기서 낸다. 약 1MB, Worker가 import한다(ADR-0008) |

**② Worker** — `worker/`. 요청마다 실행되는 JavaScript 서버 코드. 장소 탭만 쓴다(ADR-0001). 세 진입점과
브라우저 스크립트들. 데이터는 번들 JSON뿐이고 D1은 없다(ADR-0008). 경로 계산 규칙은 장소 탭 절의 용어
(도보권·환승 도보·추정 소요 시간)에 있고, 상수는 `worker/rules.js` 한 곳. 정적 자산과 같은 Worker이며
`run_worker_first`로 세 경로만 코드가 받는다. `worker/index.js`가 그 `fetch(request, env)` 진입점 하나로,
세 경로를 아래 표로 나누고 나머지는 `env.ASSETS`로 넘긴다.

| 코드 이름 | 한글 용어 | 하는 일 |
|---|---|---|
| `places` | 자동완성 후보 | `GET /places?q=검색어`. 검색어가 비었을 때만 빈 조각 — 1글자부터 후보를 낸다(2026-09-04). Kakao 로컬 REST를 대신 호출(키는 Worker 환경 변수 `KAKAO_REST_KEY`, ADR-0005), 검색 범위는 번들의 노선안 정류장 bbox + 5km(`rect`), 상위 5개를 `<ul>` 조각으로. 항목마다 이름·주소·`data-lat`·`data-lng`. 같은 검색어의 응답은 Cache API에 하루. 빈 결과·실패는 한 줄 문구 조각. 입력칸의 `hx-trigger="keyup changed delay:250ms"`가 부른다 |
| `compare` | 개편 전 카드 / 개편 후 카드 | `GET /compare?from=lat,lng&to=lat,lng`. 두 지점이 500m 이하면 "걸어갈 수 있는 거리" 조각. 아니면 노선망 둘 각각 `candidates`(지점 → 도보권 정류장, 출발은 1,000m까지 확대) → `search`(후보 쌍 → 경로, route_links로 환승 2회까지, 같은 방향 뒤 순번만) → `rank`(추정 소요 시간 → 환승 → 도보, 노선 조합 중복 제거, 기본 1 + 다른 경로 2) → `render`(→ HTML). 카드 한 쌍(머리에 상태) + 경로 지도 좌표 JSON + 다른 경로 카드용 경로 키. 판정 문장은 없다. **요청당 CPU 중앙값 20~21ms로 무료 요금제 상한 10ms를 넘는다** — 실측과 줄일 곳은 architecture §6-4 · §8 |
| `journey` | 다른 경로 카드 | `GET /journey/{id}`. 경로 키(위 「경로 키」)를 풀어 번들에서 정류장 목록·지표·좌표 JSON을 복원해 다른 경로 카드 조각으로 돌려준다. 탐색을 다시 하지 않는다 — 구간마다 방향·순번만 고른다. 저장소 없음. 키가 번들과 안 맞으면 404와 한 줄 문구 |
| `geometry` | 경로 지도 좌표 | 경로 하나를 점 목록으로 편다 — 구간마다 승차부터 하차까지 지나는 정류장 전부와 두 지점. 자리 하나에 줄이 여럿이면(길 양쪽) 앞 점에 가장 가까운 줄을 고르고, 좌표 없는 줄은 건너뛴다. `compare`와 `journey`가 함께 쓴다 |
| `place` | 장소 선택 | 브라우저 스크립트 `tools/build/place.js` → `out/place.js`. 자동완성 후보를 고르면 출발·도착 입력칸에 이름과 좌표를 확정하고, 둘 다 고르면 `/compare` 조각을 결과 영역에 끼운다. `map`과 함께 Worker 조각을 소비한다 |
| `map` | 경로 지도 · 노선 지도 | 브라우저 스크립트 `tools/build/map.js` → `out/map.js`. htmx가 조각을 끼운 뒤 `htmx:afterSwap`에서 조각 안 좌표 JSON을 읽어 Kakao JS SDK로 타일 위에 점선·실선·정류장 점을 그린다. 그리는 함수는 `window.busMap.draw(자리, 선들)` 하나이고 **좌표 배열만 받는다** — 경로도 노선도 모르므로 두 탭이 같은 함수를 부르고 입력만 다르게 준다. 장소 탭은 카드마다 고른 경로 하나를 넘기고(「지도에 표시」를 누르면 그 노선망의 경로를 갈아 끼운다), 노선번호 탭은 표 조각의 `.route-geometry`를 읽어 카드의 `.route-map`에 넘긴다 — 표 하나가 지도 하나이고 카드에 자리는 하나뿐이라 표가 바뀌면 같은 자리에 다시 그린다. JS 키는 빌드가 환경 변수 `KAKAO_JS_KEY`로 받아 껍데기에 박고, 없으면 SDK 태그를 안 단다(ADR-0005) — 그때 장소 탭은 지도 자리를 감추고(시민에게 키 이야기를 하지 않는다) 노선번호 탭은 카드에 이미 자리가 있어 「지도를 불러오지 못했습니다」 한 줄만 남긴다. Worker가 아니지만 `place`와 함께 조각을 소비한다 |

`compare`의 안은 다섯으로 나뉜다 — `network`(번들을 노선망 둘로 갈라 조회할 모습으로 바꾼다. 모듈이 처음 불릴 때 한 번. `route_links`를 양쪽으로 펴고, 그 자리에서 탈 수 있는 방향·순번과 표를 찾는 열쇠까지 미리 붙여 둔다) → `candidates`(도보권 후보 · 두 지점 사이 거리) → `search`(경로 탐색. 구간을 하나씩 이으며 직행 → 환승 1회 → 환승 2회를 한 바퀴씩 본다) → `rank`(순위와 노선 조합 중복 제거) → `render`(조각). 번들의 생김새를 아는 곳은 `network` 하나이고, 나머지 넷은 그것이 만든 모습만 본다.
