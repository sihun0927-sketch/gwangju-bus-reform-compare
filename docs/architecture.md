# 구조 (2026-09-02 설계 확정본 · 2026-09-03 장소 탭 Worker 확정)

이 문서는 세션 맥락 없이 읽혀야 한다. 용어는 `CONTEXT.md`, 결정의 근거는 `docs/adr/`.
모듈 지도: https://claude.ai/code/artifact/6977a9d4-929e-40d8-a252-9620cb46f86b (구현 때 보는 것)
화면 목업: https://claude.ai/code/artifact/fbf89d71-17a6-4f49-b963-aa3eb506b539 (용어 2026-09-03 확정본)

## 1. 한 문장

**정적 HTML 껍데기 + htmx 조각.** 페이지는 빌드 때 한 번 만들어 Cloudflare Pages에 올리고,
사용자가 무언가를 고를 때마다 htmx가 HTML 조각을 받아 그 자리에 끼운다. 조각은 두 종류다 —
노선번호 탭은 **빌드 때 만든 정적 파일**, 장소 탭은 **Worker가 번들 JSON을 읽어 만드는 동적 응답**(ADR-0008, D1 없음).

계산은 빌드 스크립트(노선번호 탭)와 Worker(장소 탭)가 하고, 브라우저는 HTML만 받는다.

## 2. 부품과 역할

| 부품 | 무엇 | 이 프로젝트에서 하는 일 | 어디에 |
|---|---|---|---|
| **Cloudflare Pages** | 정적 파일 창고 | `out/` 전체를 냄. 껍데기, 노선 개편 목록 표, 노선번호 탭 조각 308개(카드 103 + 표 205), CSS 한 장, 브라우저 스크립트 둘(`place.js` · `map.js`) | 배포 |
| **Cloudflare Worker** | 요청마다 실행되는 JS 서버 | 장소 탭 셋: `GET /places?q=`(Kakao REST 대리 호출) · `GET /compare?from=&to=`(경로 계산 → 카드 한 쌍) · `GET /journey/{id}`(다른 경로 카드 + 경로선 좌표). 정적 자산과 같은 Worker, `run_worker_first` 세 경로 | 배포 |
| **번들 JSON** | Worker와 함께 배포되는 데이터 파일 `worker/data.json` | 정류장(줄 단위)·노선망·환승 쌍·노선 쌍별 환승 지점. 약 1MB. Worker만 읽는다. ~~D1~~은 쓰지 않는다(ADR-0008) | Worker 번들 |
| **빌드 스크립트** | 배포 전 한 번 도는 프로그램 | `data/source/`의 CSV 7개(시 공표 6 + `stops.csv`) → 정적 조각 파일 + 번들 JSON. 노선 변화 표 규칙과 환승 쌍·노선 쌍 계산이 여기 | `tools/` |
| **htmx** | `hx-*` 속성 라이브러리 | 버튼·목록 줄에 붙어 조각 URL을 받아 `hx-target`에 끼움. 페이지 새로고침 없음. 껍데기가 CDN 스크립트 태그 하나로 부르고 SRI로 고정한다 | 브라우저 |
| **Kakao JS SDK** | 지도 | 지도 타일과 경로선·점을 **브라우저에서** 그림. htmx는 지도를 못 그린다. JS 키는 브라우저에 노출, 도메인 화이트리스트로 방어(ADR-0005) | 브라우저 |
| **Kakao REST API** | 장소 검색 | Worker가 대신 호출. REST 키는 Worker 환경 변수에만 둔다(ADR-0005) | Worker |

## 3. 두 탭의 정적 / 동적 분할

번호는 캔버스 아트보드의 번호와 같다. 회색 실선 = 정적, 초록 점선 = 동적.

### 3-1. 장소로 찾기 (캔버스 페이지 1)

| 번호 | 영역 | 정적/동적 | URL | 만드는 곳 |
|---|---|---|---|---|
| ① | 껍데기: 제목·탭·출발/도착 입력칸·안내 | 정적 | `index.html` | 빌드 |
| ④ | 자동완성 후보 (장소, 입력 중) | 동적 | `GET /places?q=전남대` → `<ul>` | Worker → Kakao REST |
| ⑤ | 개편 전 카드 / 개편 후 카드 (머리에 상태, 판정 문장 없음) | 동적 | `GET /compare?from=lat,lng&to=lat,lng` | Worker → 번들 JSON → 경로 계산 |
| ⑥ | 다른 경로 카드 + 경로 지도 좌표 | 동적 | `GET /journey/{id}` | Worker. 선 긋기는 Kakao JS |

흐름: 껍데기 → 입력 → ④ → 선택 → ⑤ → 펼침 → ⑥. 예시 쌍 버튼은 두지 않는다(캔버스에서 삭제).

### 3-2. 노선번호로 찾기 (캔버스 페이지 2)

| 번호 | 영역 | 정적/동적 | URL | 만드는 곳 |
|---|---|---|---|---|
| ① | 껍데기: 노선번호 입력칸·안내 | 정적 | `index.html` | 빌드 |
| ② | 노선 개편 목록 표 103행 (개편 전 · 개편 후) | 정적 | `index.html` 안에 통째로 | 빌드 |
| ④ | 자동완성 후보 (번호 103개, 값 = 번호 · 설명 = 대체 노선) | 정적 | `index.html` 안 `<datalist>`. 고르면 htmx `path-params` 확장이 `route/{number}.html`을 부름 | 빌드 |
| ⑤ | ~~표 필터~~ | — | 없음(2026-09-04). 표는 늘 103행, 좁히기는 ④가 맡는다 | — |
| ⑥ | 노선 변화 카드: 제목·대체 노선·방면 선택·대체 노선 선택·요약 칸·노선 지도 자리 | **정적 조각** | `route/{번호}.html` | 빌드 (ADR-0002) |
| ⑦ | 노선 변화 표 + 노선 지도 좌표 JSON(상행, 줄마다 상태) | **정적 조각** | `route/{번호}/{개편 전 방면}/{대체 노선}/{개편 후 방면}.html` | 빌드 (ADR-0002, 0003, 0006) |

흐름: 입력 → ④ → 번호 확정 → ⑥ → 방면·대체 노선 고름 → ⑦ → `map`이 ⑦의 좌표로 노선 지도를 그림.
조망 지도(버스가 오지 않는 정류장)는 두지 않는다(캔버스에서 삭제). **이 탭은 Worker 없이 돈다** — 브라우저
스크립트는 `map` 하나(두 탭 공용)뿐이다.

## 4. 데이터 흐름

```
data/back_up/api_stops.json (API 응답 원본, ADR-0007)
        │  tools/build_stops.py
        ▼
data/source/*.csv (시 공표 6 + stops.csv 좌표 4,746개)   data/name_canon.json (이름 잇기 8)
        │                                                       │
        └──────────────────────┬────────────────────────────────┘
                               ▼
              tools/build.*  (빌드 스크립트)
        ┌──────────────┴───────────────────────────┐
        ▼                                          ▼
 out/                                       worker/data.json (번들 JSON)
   index.html        ← 껍데기 + 노선 개편 목록 표      정류장 · 노선망 · 환승 쌍 · 노선 쌍
   route/문흥18.html ← ⑥ 결과 카드                       │
   route/문흥18/기본/간선18/기본.html ← ⑦ 표              ▼
   route/문흥18/기본/지선10/기본.html               Worker(import) ◄── htmx (장소 탭)
   … (카드 103 · 표 205, ADR-0006)
        │
        ▼
 Cloudflare Pages ◄── htmx (노선번호 탭)
```

빌드 스크립트의 단계(노선 변화 표 부분, ADR-0003):

0. **번호 잇기** — 노선 표기를 (종류, 번호, 방면)으로 갈라 비교표와 노선안을 잇는다(ADR-0006). 못 찾는 쌍 0
1. **명칭 사전** — `명칭 변경 정류소.csv`에서 옛 이름 → 새 이름 집합. 옛 이름 하나에 새 이름 여럿 허용
2. **방면 분리** — 같은 종류+번호의 행을 방면으로 나눔. 개편 전 6개 번호 14행, 개편 후 지선97
3. **기·종점 정렬** — 개편 전 방면 × 대체 노선 × 개편 후 방면 쌍(205)마다 ① 기·종점 이름 → ② 정류장 겹침 → ③ `data/기종점정렬표.csv`
4. **기·종점 정류장 대조** — 상행끼리·하행끼리 최장 공통 부분열(LCS)로 정렬. 명칭 사전을 적용해 이름이 다른 같은 정류장을 「유지」로
5. **비고** — 명칭 변경 · 통폐합 · 이전 · 신설 (CSV 사실만. 상태는 적지 않는다)
6. **파일 쓰기** — `route/{번호}.html`, `route/{번호}/{개편 전 방면}/{대체 노선}/{개편 후 방면}.html`

## 5. 노선 변화 표 규격

캔버스 페이지 3에 문흥18 ↔ 간선18 실제 값으로 그려져 있다.

| 항목 | 규격 |
|---|---|
| 열 | `# · 개편 전 상행 정류장 · 개편 후 상행 정류장 · 개편 전 하행 정류장 · 개편 후 하행 정류장 · 비고` |
| 줄 | 정류장 이름을 순서대로 맞댄 것. 위에서 아래로 읽으면 버스가 가는 순서 |
| 색 | 개편 전에만 있는 줄 = `--worse-bg`(붉은 바탕), 개편 후에만 = `--better-bg`(초록 바탕). 글자 굵기는 같다 |
| 상태 | 유지 / 경유 제외 / 경유 추가. 주어는 노선 — 정류장이 없어진 것이 아니다. 화면에는 줄 색·범례·요약 칸에만 나온다 |
| 상행·하행 요약 칸 | 상행·하행 각각 유지/경유 제외/경유 추가 셋 = 여섯 칸, 같은 크기로 나란히. "상행 · 경유 제외 11곳" |
| 버튼 줄 | 개편 전 방면 선택(6개 노선만) / 대체 노선 선택 `[간선18] [지선10]` / 개편 후 방면 선택(대체 노선에 방면이 있을 때만) |
| 하행 없는 노선 | 하행 두 칸을 비우고 비고에 "순환 노선 · 하행 없음" 또는 "편도 운행 · 하행 없음". 열은 늘 여섯 |
| 방향 표시 | 기·종점 정렬에서 뒤집어 맞댄 경우, 표 아래에 "기점·종점이 반대라 뒤집어 맞댔습니다" 한 줄 |
| 제목 | 「<번호(방면)> 노선 변화」. 예: 「두암81(각화초교.장등동) 노선 변화」 |
| 출처 줄 | "출처: 광주광역시 시내버스 노선개편안 「노선개편 전·후 비교표」 · 「광주권역 개편전/개편후 노선안」" |

## 6. 실측 수치 (2026-09-03, `tools/measure_direction.py`)

한 번 잰 값을 단정해 쓰지 않는다. 스크립트를 다시 돌려 확인한다.

| 항목 | 값 |
|---|---|
| 개편 전 노선안 행 / 번호 | 111 / 103 (방면 갈라진 번호 6개: 228·두암81·상무62·선운101·송정97·지원152, 14행) |
| 개편 후 노선안 행 / 종류+번호 | 119 / 118 (방면 갈라진 것 지선97 하나) |
| 비교표 행 / 번호 쌍 / 표 파일 수 | 103 / 187 / **205** (개편 전 방면 × 개편 후 방면까지 가른 쌍. 대체 0개 1행, 1개 45, 2개 43, 3개 10, 4개 1, 7개 2, 8개 1) |
| 기·종점 정렬 ① 기·종점 이름 자동 | 92쌍 |
| 기·종점 정렬 ② 정류장 겹침 자동(차이 뚜렷) | 95쌍 |
| ③ 사람이 적어야 함 (겹침 약함 16 + 동률 2) | **18쌍** — 첨단92·첨단94·송정99처럼 대체가 여럿이라 하나하나는 조금씩만 겹치는 경우, 그리고 선운101 두 방면 × 지선97(빛그린산단출근) |
| 번호 못 찾음 | 0 (ADR-0006 규칙). 옛 규칙(숫자만 남김)은 11쌍을 못 찾고 5쌍을 잘못 붙였다 — 급행03·05·06이 간선에, 「97」이 출근 변형에 |
| 하행 0개(순환) | 개편 전 7행, 개편 후 1행 |
| 문흥18 ↔ 간선18 | 상행 유지 49 · 경유 제외 9 · 경유 추가 3 / 하행 52 · 10 · 4 (명칭 사전 적용 전 47·11·5 / 50·12·6). 표를 바꾸는 명칭 변경은 **2건** — 문흥고가 → 문화육교, 하남2지구 → 시립하남도서관 |
| 문흥18에서 명칭 변경 CSV에 있는 정류장 | 4개. 둘은 표를 안 바꾼다 — 광주역육교는 개편 후 노선안이 옛 이름을 그대로 쓰고, 현대위아(→ 모비언트)는 새 이름이 간선18에 없어 경유 제외다 |
| 노선 지도 (2026-09-04, `route_geometry`) | 점 14,808개 · 좌표가 없어 못 찍은 정류장 **52곳**(그 52곳이 205개 표 중 85개에 걸쳐 206줄로 나온다 — 표의 「N곳」은 줄이 아니라 정류장을 센다) · 조각 하나 평균 23KB(절반이 좌표) |
| 같은 이름이 여러 곳 | 노선안 정류장 이름 1,653개 중 46개가 `stops.csv`에서 300m 넘게 흩어져 있다(「금곡마을」 6줄 55km — 광주와 전남에 같은 이름). 줄을 다 평균 내면 **205개 표 중 130개**에 3km 넘는 튐이 생긴다. 노선을 따라 가장 짧아지는 자리를 고르면 **23개**로 줄고, 남는 것은 나주·화순으로 곧장 가는 직행·급행의 진짜 먼 구간이다 |
| 문흥18 ↔ 간선18 지도 | 개편 전 선 58점 · 대체 노선 선 52점(뒤집힌 쌍이라 간선18 **하행**) · 점 61개(유지 49 · 경유 제외 9 · 경유 추가 3) · 못 찍은 것 0 |
| 비고가 붙은 칸 (205개 표 전체) | 명칭 변경 688 · 신설 정류소 433 · 폐지 96 · 이전 2 · **통폐합 0**. `title`(사유)이 붙은 칸 98 |
| 통폐합 비고가 0인 까닭 | 통폐합 4행이 전부 「계수초교(상무한국아파트)」 꼴이라 노선안 이름과 안 맞는다. 흡수된 쪽(서방사거리육교·상무한국아파트·오치한전(북))은 노선안에 있으므로 이름 대조를 넓히면 붙는다 — 다음 스펙 |

## 7. 결정 요약 (그릴링 2026-09-02)

| 번호 | 질문 | 결정 | ADR |
|---|---|---|---|
| Q1 | 정류장 좌표 출처 | ~~이전 리포의 `data/stops.json` 재사용~~ → 광주 BIS API, `data/source/stops.csv` 4,746개 | ~~0004~~ **0007** |
| Q2 | DB 형태 | ~~장소 탭은 Worker + D1~~ → Worker 번들 JSON, D1 없음(2026-09-03) | ~~0001~~ **0008** |
| Q3 | 이름 바뀐 정류장 | 같은 정류장. 옛 이름 하나 → 새 이름 여럿 사전 | 0003 |
| Q3' | 기·종점 정렬 | 기·종점 이름 → 정류장 겹침 → 남는 쌍만 `기종점정렬표.csv`(2026-09-03 재측정 18쌍) | 0003 |
| Q4 | 대체 노선 여럿 | 번호 버튼(대체 노선 선택)으로 하나 골라 표 하나씩 | 0003 |
| Q5 | 비고 | 명칭 변경 · 통폐합 · 이전 · 신설. 「대신 서는 대체 노선」은 2026-09-03에 뺌 | 0003 |
| Q6 | 노선번호 탭 조각 출처 | 빌드 때 정적 파일 | 0002 |
| Q7 | 하행 없는 노선 | 5열 유지, 하행 칸 비움 | 0003 |

그릴링 2026-09-03 (번호 잇기):

| 번호 | 질문 | 결정 | ADR |
|---|---|---|---|
| Q1 | 급행 03 vs 간선 03 | 개편 후 노선은 종류+번호. 비교표에 숫자만이면 급행 아닌 것 | 0006 |
| Q2 | 비교표의 「1」 | 앞자리 0 무시 규칙. 교정표는 안 둠 | 0006 |
| Q3 | 지선97(빛그린산단출근) | 개편 후에도 방면. 카드에 「개편 후 방면 선택」 줄, 편도 비고 | 0006 |
| Q4 | 화면·파일 표기 | 노선 이름 = 종류+번호(+방면), 띄어쓰기 없음 `[간선18]` | 0006 |
| Q5 | 표 조각 경로 | 늘 네 단계 `route/{번호}/{개편 전 방면}/{대체 노선}/{개편 후 방면}.html` | 0006 |

그릴링 2026-09-03 (장소 탭 Worker, 한 번에 한 질문):

| 번호 | 질문 | 결정 | ADR |
|---|---|---|---|
| Q1 | 경로 계산 위치 | 빌드가 노선 단위 구조를 미리 계산, Worker는 조회 | 0008 |
| Q2 | 환승 상한 | 최대 2회 | — |
| Q3 | 도보권 | 출발·도착 500m, 8개. 출발지만 100m씩 1,000m까지 확대. 환승 도보 350m | — |
| Q4 | 순위·지표 | 추정 소요 시간(정류장당 20초 · 도보 4km/h, 배차 제외) → 환승 → 도보. 예상 시간 카드 표시. 배차는 개편 전 파일 반입 예정 | — |
| Q5 | 좌표 없는 57개 | 앞뒤 중점의 추정 좌표로 후보·통과 모두 허용. 파일에는 안 씀 | 0007 개정 |
| Q6 | Worker 언어 | JavaScript | — |
| Q7 | Kakao 검색 범위 | 노선안 정류장 bbox + 5km. 키는 Cloudflare 설정에 있음 | 0005 |
| Q8 | 정류장 정체성 | stops.csv 줄 단위, 키 STATION_NUM | 0008 |
| Q9 | 데이터 저장 | 다섯 표 전부 Worker 번들 JSON, route_links는 쌍당 최단 1개, D1 없음 | 0008 |
| Q10 | 방향·순환 | 같은 방향 뒤 순번만, 순환 넘김 불허 | — |
| Q11 | 판정 문장 | 두지 않는다. 카드 머리에 상태(직행·환승 1회·환승 2회·경로 없음) | — |
| Q12 | 경로 키 | 구성 요소를 base64url로, 저장소 없음 | — |
| Q13 | 다른 경로 | 노선 조합이 다른 것 2개 | — |
| Q14 | `/places` | 하루 캐시 · 2글자부터 · 5개 | — |
| Q15 | 두 지점 500m 이하 | 경로 없이 "걸어갈 수 있는 거리" 조각 | — |

캔버스에서 사용자가 직접 지운 것: 장소 탭 예시 쌍 버튼, 정적 자원 표시, 노선번호 탭 조망 지도.
캔버스·`docs/images/modmap-1-place-*.png`·`mock-1-place-result.png`는 D1과 판정 줄이 남아 있어 **갱신 대상**.

### 7-3. 그릴링 2026-09-04 (노선번호 탭 잔여)

| 번호 | 질문 | 결정 | ADR |
|---|---|---|---|
| Q1 | 번호로 좁히기 | `<datalist>` 103개 + htmx `path-params`. 표 필터 없음, 우리 스크립트 없음 | — |
| Q2 | 후보 한 줄 | 값 = 번호, 설명 = 대체 노선 이름들. 목록에 없는 값은 안내문 한 줄 | — |
| Q3 | 통폐합 비고 | 「A(B)」의 B가 개편 전 열에 나오는 줄에 "통폐합: A에 흡수". 노선안은 고쳐 읽지 않음 | 0003 |
| Q4 | 기종점정렬표 18행 | 겹침이 0~9/50곳뿐이라 값 그대로 두고 「확인」 열을 채움(같은 날 완료) | — |
| Q5 | 노선 지도 | 장소 탭보다 먼저 짓는다 | — |
| Q6 | 좌표 JSON 위치 | 카드가 아니라 표 조각 ⑦ (표 하나 = 지도 하나) | — |
| Q7·8 | 지도 그림 | 상행만. 개편 전 굵은 초록 아래 + 대체 노선 가는 파랑 위. 점 유지 회색·제외 빨강·추가 파랑. 표 색은 그대로 | — |
| Q9 | 작업 단위 | PR 넷 (§9-8) | — |

## 8. 미결

- **`절골입구`는 `(북)`으로 둔다.** `(서)`와 약 40m라 도보권 500m·환승 도보 350m 판정을 바꾸지 않는다(2026-09-03 확인).
- **`stops.csv`의 이름·ARS_ID가 노선안·명칭 변경 CSV와 어긋나는 자리.** 셋을 확인했다(2026-09-04).
  ① 같은 이름이 여러 고을에 있다(§6) — 노선 지도는 노선을 따라 고르지만 `bundle`의 `route_stops`는
  아직 그 이름의 줄을 **전부** 달아 두므로, 장소 탭 경로 계산이 이 이름을 만나면 다시 봐야 한다.
  ② 명칭 변경 CSV의 ID가 `stops.csv`의 다른 정류장을 가리키는 행이 있다 — 돈보스코학교 → 하남고등학교의
  ID 5936은 `stops.csv`에서 「비아동주민센터(앞)」이고, 옳은 줄은 ID 5964뿐이다(5km 차이).
  ③ 이름이 `stops.csv`에 한 줄뿐인데 그 줄이 노선에서 멀리 떨어진 것이 있다(지선92의 「석계」, 왕복 20km).
  고를 것이 하나뿐이라 지도가 바로잡을 수 없다. 시 자료나 BIS가 고쳐지면 다시 본다
- **신설 정류소 57개의 좌표.** `source/stops.csv`에 없다(신설 56 + `광주교대역2번출구`). 개편 후 BIS가 열리면
  다시 받고, 그때까지는 비워 둔다 — 지어낸 좌표를 넣지 않는다(ADR-0007)
- **서방사거리육교의 어긋남.** 통폐합 CSV는 계림사거리에 흡수·폐지됐다고 적었는데 개편 후 노선안(간선19 · 간선39 · 419)에는
  그대로 있다. 노선안을 고쳐 읽지 않고 비고만 붙인다(§7-3 Q3). 시가 자료를 고치면 다시 본다

## 9. 다음 할 일

1. ~~`tools/measure_direction.py`를 돌려 §6 수치가 재현되는지 본다~~ (2026-09-03 완료)
2. ~~번호 정규화 규칙을 정하고 못 찾는 11쌍을 0으로 만든다~~ (ADR-0006, 못 찾음 0)
3. ~~빌드 스크립트의 노선 변화 표 부분(§4 단계 0~6)을 테스트부터 쓴다~~ (2026-09-03 완료, 표 205 · 카드 103)
4. ~~`route/문흥18.html`·`route/문흥18/기본/간선18/기본.html`을 만들어 정적 껍데기에 htmx로 끼워 본다~~
   (2026-09-03 완료 — `out/index.html`에서 줄 → 카드 → 표까지 새로고침 없이 돈다)
5. ~~캔버스 목업 2·3·4의 조각 주소·「간선 18」 띄어쓰기를 ADR-0006 표기로 고치고 PNG를 다시 뽑는다~~ (2026-09-03 완료)
6. ~~정류장 좌표를 반입한다~~ (2026-09-03 완료, ADR-0007 — `data/source/stops.csv` 4,746개·결측 0)
7. ~~장소 탭 Worker 그릴링~~ (2026-09-03 완료, §7 표 · ADR-0008)
8. 노선번호 탭 잔여(§7-3, 2026-09-04) — ①~③ 완료(`<datalist>` · 통폐합 비고 · 노선 지도 `route_geometry` + `map`).
   남은 것은 ④ 캔버스 `modmap-2-route-map` PNG 갱신과 **Kakao JS 키가 있는 환경에서의 눈 확인** — 리포에 키가
   없어 빌드도 테스트도 키 없이 돌고, 그때 `map.js`는 지도 자리에 「지도를 불러오지 못했습니다」만 남긴다
9. 빌드 `bundle` 단계 — transfers·route_links를 만들고 `worker/data.json` 크기와 줄 수를 실측한다(다른 세션에서 진행 중)
10. Worker 셋(`places` · `compare` · `journey`). `map`은 8-③에서 먼저 생긴다. `compare`의 CPU 시간이 첫 실측
11. 캔버스·PNG의 D1·판정 줄을 지운다
